/* eslint no-unused-vars: ["error", { "argsIgnorePattern": "next" }] */

const log = require('debug')('randomizer.controller');

export default function random() {
  log('Welcome to Randomizer random API Endpoint niggato');
}
